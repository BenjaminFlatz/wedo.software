# Copy this file to .env for local `docker compose up` usage.
# Never commit a real .env file.

# --- Frontend (build-time Vite variables, services/frontend) -----------
# Chatwoot website token is not a secret (it ships in the client bundle),
# so both variables are safe to keep as plain defaults.
VITE_CHATWOOT_BASE_URL=https://chatwoot.wedo-software.com
VITE_CHATWOOT_WEBSITE_TOKEN=JnugYLngRwnCRdc3oW2G76t2
