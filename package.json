{
  "name": "wedo-software-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for wedo-software.com, built with precompiled Tailwind CSS and JSX.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/tailwind.css -o ./dist/tailwind.css --minify",
    "build:js": "node ./scripts/build-js.mjs",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tailwindcss": "^3.4.19"
  }
}
